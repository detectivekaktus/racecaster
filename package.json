{
  "name": "racecaster",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:server": "npx tsc --project tsconfig.server.json && cp src/index.html out/index.html && mkdir -p out/styles && cp src/styles/style.css out/styles/style.css",
    "build:client": "npx tsc --project tsconfig.client.json",
    "build": "npm run build:server && npm run build:client",
    "start": "npm run build && node out/index.js",
    "clean": "rm -r out/"
  },
  "keywords": [],
  "author": "Artiom Astashonak",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "@types/node": "^22.9.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
