{
  "name": "racecaster",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "build:server": "npx tsc --project tsconfig.server.json && cp src/server/index.html out/server/index.html && mkdir -p out/server/styles && cp src/server/styles/style.css out/server/styles/style.css",
    "build:client": "npx tsc --project tsconfig.client.json",
    "build": "npm run build:server && npm run build:client",
    "start": "npm run build && node out/server/index.js",
    "clean": "rm -r out/"
  },
  "keywords": [],
  "author": "Artiom Astashonak",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "@types/node": "^22.9.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
